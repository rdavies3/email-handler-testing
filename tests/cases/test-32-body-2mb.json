{
  "id": "32",
  "name": "Email body ~2MB creates Case and EmailMessage",
  "category": "body-size",
  "description": "Verifies that an email with a body of approximately 2MB (2,097,152 bytes) creates a Case and an associated EmailMessage record.",
  "emailProperties": {
    "to": "{{primary_email}}",
    "subject": "Test-32-{{timestamp}}",
    "textBody": "{{body_generator:size=2097152}}",
    "htmlBody": null,
    "attachments": [],
    "inlineImages": [],
    "fromNameOverride": null
  },
  "sendMethod": "eml",
  "preconditions": [],
  "verification": {
    "type": "case-created",
    "queries": [
      {
        "object": "Case",
        "filter": "Subject = 'Test-32-{{timestamp}}'",
        "assertions": [
          { "field": "Subject", "operator": "equals", "value": "Test-32-{{timestamp}}" },
          { "field": "Id", "operator": "not_null" }
        ]
      },
      {
        "object": "EmailMessage",
        "filter": "ParentId = '{{caseId}}'",
        "assertions": [
          { "field": "ParentId", "operator": "not_null" }
        ]
      }
    ]
  }
}
